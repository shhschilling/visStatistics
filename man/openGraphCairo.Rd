% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/openGraphCairo.R
\name{openGraphCairo}
\alias{openGraphCairo}
\title{Cairo wrapper function}
\usage{
openGraphCairo(
  width = 640,
  height = 480,
  file = NULL,
  type = NULL,
  pointsize = 12,
  bg = "transparent",
  canvas = "white",
  units = "px",
  dpi = 150,
  mag = 1
)
}
\arguments{
\item{width}{see \code{Cairo()}}

\item{height}{see \code{Cairo()}}

\item{file}{name of file to be created or connection to write to  without file extension ".\code{type}". Default file name "visstat".}

\item{type}{see \code{Cairo()}}

\item{pointsize}{see \code{Cairo()}}

\item{bg}{see \code{Cairo()}}

\item{canvas}{see \code{Cairo()}}

\item{units}{see \code{Cairo()}}

\item{dpi}{DPI used for the conversion of units to pixels. Default value 150.}

\item{mag}{multiplies both \code{width} and \code{height} by magnification factor \code{mag}.}
}
\value{
NULL, if no type is specified. Otherwise see \code{Cairo()}
}
\description{
Cairo wrapper function retuning NULL if not \code{type} is specified
}
\details{
\code{openGraphCairo()} \code{Cairo()} wrapper function. Differences to \code{Cairo:}
a) prematurely ends the function call to \code{Cairo()}  returning NULL, if no output \code{type} is provided.
b) \code{file}should not include file extension "."\code{type}. File name is generated by pasting \code{file}  with "." \code{type}extension.
c)  magnifies graphics device output by factor \code{mag}.
}
\examples{
# return NULL
openGraphCairo()

# very simple KDE (adapted from example in \code{Cairo()})
openGraphCairo(type="png",mag=2.54) #mag is conversion factor cm to inch
plot(rnorm(4000),rnorm(4000),col="#ff000018",pch=19,cex=2) # semi-transparent red
dev.off() # creates a file "visstat_plot.png" with the above plot
# remove the example files if not in an interactive session
if (!interactive()) unlink(c("visstat_plot.png","plot.pdf"))
}
