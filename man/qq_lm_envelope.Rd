% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qq_lm_envelope.R
\name{qq_lm_envelope}
\alias{qq_lm_envelope}
\title{Simulated Q-Q envelopes for linear model residuals}
\usage{
qq_lm_envelope(
  model,
  conf.level = 0.95,
  nsim = getOption("visStatistics.qq_nsim", 5000L),
  q.type = 2L,
  tol = 1e-04,
  max.iter = 100L
)
}
\arguments{
\item{model}{An unweighted \code{lm} or \code{aov} object.}

\item{conf.level}{Numeric confidence level for the envelopes.}

\item{nsim}{Integer number of simulated refits.}

\item{q.type}{Integer quantile type passed to \code{stats::quantile()}.}

\item{tol}{Numeric tolerance for the simultaneous-band coverage search.}

\item{max.iter}{Integer maximum number of bisection iterations.}
}
\value{
A list of class \code{qq_lm_envelope} with the observed sorted
  residuals, theoretical quantiles, pointwise bounds, simultaneous bounds,
  the simulated sorted residual matrix, and the achieved simultaneous
  coverage.
}
\description{
Computes pointwise and simultaneous Q-Q envelopes for internally
studentised residuals from an unweighted \code{lm} or \code{aov} object by
repeatedly simulating responses from the fitted normal-error model and
refitting the model. The simultaneous envelope follows the Monte Carlo
tolerance-band idea of \enc{Schützenmeister}{Schuetzenmeister} et al.
(2012); see
\code{\link{vis_lm_assumptions}} and \code{\link{visstat}} for where these
bands are drawn.
}
\examples{
fit <- lm(mpg ~ wt, data = mtcars)
env <- qq_lm_envelope(fit, nsim = 100)
str(env)

}
\references{
\enc{Schützenmeister}{Schuetzenmeister}, A., Jensen, U., & Piepho, H.-P.
(2012). Checking
Normality and Homoscedasticity in the General Linear Model Using
Diagnostic Plots. \emph{Communications in Statistics - Simulation and
Computation}, 41(2). doi:10.1080/03610918.2011.582560.
}
