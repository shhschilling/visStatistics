## Personal dictionary for R CMD check's aspell pass.
## inst/WORDLIST serves the `spelling` package; this file serves R CMD check.
description <- list(encoding = "UTF-8", dictionaries = "words")
Rd_files    <- list(encoding = "UTF-8", dictionaries = "words")
vignettes   <- list(encoding = "UTF-8", dictionaries = "words")
R_files     <- list(encoding = "UTF-8", dictionaries = "words")
